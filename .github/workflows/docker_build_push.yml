name: Docker Build and Push
on:
  push:
    branches: [main]
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{vars.DOCKERHUB_USERNAME}}
          passowrd: ${{secrets.DOCKERHUB_TOKEN}}
      - name: Docker build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{DOCKERHUB_USERNAME}}/github-actions-app:${{ github.ref_name }}
            ${{DOCKERHUB_USERNAME}}/github-actions-app:latest
            ${{DOCKERHUB_USERNAME}}/github-actions-app:${{ github.sha }}
